/* Place in /etc/systemd/system/ */

[Unit]
Description=Block Builder Based off Flashbots

[Service]
Type=simple
User=validator
Restart=always
ExecStart=/usr/local/bin/builder --builder.secret_key $BUILDER_SECRET_KEY --config /ethereum/builder.toml

[Install]
WantedBy=default.target
